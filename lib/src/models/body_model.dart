import 'package:flutter/material.dart';

class EasyBody {
  /// The actual body of the dashboard
  final Widget child;

  /// title that will appear on the app bar
  final Widget title;

  /// widget to show on the body
  EasyBody({required this.child, required this.title});
}
